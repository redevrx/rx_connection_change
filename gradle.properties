#Kotlin
kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx3072M
#Gradle
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.caching=true
#Android
android.nonTransitiveRClass=true
android.useAndroidX=true

# Publishing credentials do NOT belong in this file — it is tracked by git.
# Put ossrhUsername / ossrhPassword / signing.keyId / signing.password /
# signing.secretKeyRingFile in ~/.gradle/gradle.properties instead; Gradle merges
# that file in automatically and it never leaves the machine.
#
# The values that used to sit here were published to GitHub and have been scrubbed
# from history. Treat them as compromised and rotate them, not reuse them.
